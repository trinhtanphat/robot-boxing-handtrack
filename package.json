{
  "name": "robot-boxing-handtrack",
  "version": "0.1.0",
  "description": "Interactive 3D robot boxing arena controlled by webcam hand and body tracking.",
  "private": false,
  "type": "module",
  "scripts": {
    "generate:assets": "node scripts/generate-model-assets.mjs",
    "predev": "npm run generate:assets",
    "dev": "vite",
    "prebuild": "npm run generate:assets",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "check": "npm run test && npm run build"
  },
  "dependencies": {
    "@mediapipe/tasks-vision": "^1.0.1",
    "three": "^0.186.0"
  },
  "devDependencies": {
    "vite": "^8.3.0",
    "vitest": "^5.0.0"
  }
}
